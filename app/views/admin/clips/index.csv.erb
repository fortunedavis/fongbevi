
<% fields = [:id, :audio, :sentence_id] %>
<%= fields.map(&:to_s).join(";") %>
<% @clips.each do |clip| %>
<%= clip.sentence.content%>
<%= clip.audio_blob%>
<%= fields.map { |field| clip[field].to_s }.join(";") %>
<% end %>